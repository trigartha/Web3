import 'mingo/lib/polyfill'
import { OP_AGGREGATE, OP_GROUP, OP_PIPELINE, OP_PROJECTION, OP_QUERY } from 'mingo/lib/constants'
import { _internal, setup } from 'mingo/lib/internal'
import { Query, find, remove } from 'mingo/lib/query'
import { Aggregator, aggregate } from 'mingo/lib/aggregator'
import { CollectionMixin } from 'mingo/lib/mixin'
import { Cursor } from 'mingo/lib/cursor'
import { addOperators } from 'mingo/lib/operators'

const VERSION = '1.3.3'

// mingo!
export default {
  _internal,
  Aggregator,
  CollectionMixin,
  Cursor,
  OP_AGGREGATE,
  OP_GROUP,
  OP_PIPELINE,
  OP_PROJECTION,
  OP_QUERY,
  Query,
  VERSION,
  addOperators,
  aggregate,
  find,
  remove,
  setup
}
